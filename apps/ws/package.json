{
  "name": "ws",
  "version": "1.0.0",
  "scripts": { 
    "build": "tsc -b",
    "dev": "npm run build && npm run start",
    "start": "node dist/index.js"
   },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "tsx": "^4.0.0"
  }
}