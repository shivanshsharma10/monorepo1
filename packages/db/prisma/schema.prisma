generator client {
  provider = "prisma-client-js"
  // This output path ensures the client is inside your package, making it portable.
  output   = "../src/generated/prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Member {
  id    String @id @default(uuid())
  email String @unique
  name  String?
}